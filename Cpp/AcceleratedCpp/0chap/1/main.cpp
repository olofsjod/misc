#include <iostream>

int main() {
  std::cout << 3 + 4 << std::endl; // Evaluates take two integers and applies
                                   // the binary '+' operator on it, returning one integer
}
